import drivers
import drivers/bga

def empty() = NoFramebufferState()

def init() = {
  // TODO: only if BGA is detected
  bga::init()
}

// TODO: better Output effect subtraction
def handler { prog: => Nothing / Output }: Nothing / Global = {
  // TODO: only if BGA is detected
  with bga::handler
  prog()
}

// --- syscalls ---

def context() = {
  // TODO: only if BGA is detected
  bga::context()
}
