import x86/pic
import x86/serial
import x86/commands
import x86/interrupts
import framebuffer
import interrupt

import panic

import drivers
import capabilities/timer
import capabilities/graphics
import capabilities/keyboard
import capabilities/mouse

import graphics/framebuffer

import calculator
import utils

import scanner

def header(): Unit / Output = {
  val logo = """
  _____     ______  __  __     _    _    ____   _____
 (_____)   |  ____|/ _|/ _|   | |  | |  / __ \ / ____|
   ___     | |__  | |_| |_ ___| | _| |_| |  | | (___
  (___)    |  __| |  _|  _/ _ \ |/ / __| |  | |\___ \
  _____    | |____| | | ||  __/   <| |_| |__| |____) |
 (_____)   |______|_| |_| \___|_|\_\\__|\____/|_____/
"""
  logo.each
}

def fault(fault: Fault) = {
  panic(s"""
=============
=== FAULT ===
=============

${fault.pretty}
""")
}

def output { os: => Nothing / Output }: Nothing = {
  with emit[Char, Nothing] { ch => framebuffer::print(ch.show) }
  // with redraw
  os()
}

def os { inputs: => Nothing / Input }: Nothing / Output = {
  with looping

  calculator::program() {inputs}
}

def input { requests: => Nothing / Request }: Nothing / Input = {
  with statefully[KeyboardState](empty().ref)
  with statefully[MouseState](empty().ref)
  with statefully[TimerState](empty().ref)
  with statefully[FramebufferState](empty().ref)

  timer::init()
  keyboard::init()
  mouse::init()
  graphics::init()

  with interrupt::on[TimerTick].handle[TimerTick, TimerState] { timer::handler }
  with interrupt::on[PS2Keyboard].handle[PS2Keyboard, KeyboardState] { keyboard::handler }
  with interrupt::on[Cascade].ignore
  with interrupt::on[COM2].ignore
  with interrupt::on[COM1].ignore
  with interrupt::on[LPT2].ignore
  with interrupt::on[FloppyDisk].ignore
  with interrupt::on[LPT1].ignore
  with interrupt::on[CMOS].ignore
  with interrupt::on[Peripheral1].ignore
  with interrupt::on[Peripheral2].ignore
  with interrupt::on[Peripheral3].ignore
  with interrupt::on[PS2Mouse].handle[PS2Mouse, MouseState] { mouse::handler }
  with interrupt::on[FPU].ignore
  with interrupt::on[PrimaryATA].ignore
  with interrupt::on[SecondaryATA].ignore
  with dispatch
  requests()
}

def loop(): Nothing = {
  serial::init()
  // header()

  // interrupts
  val inbox = interrupts::init(box fault)
  pic::init()

  // capabilities
  with output
  with os
  with input
  interrupts::idle(inbox)
}

def main() = {
  loop()
  panic("loop returned?!")
  ()
}
