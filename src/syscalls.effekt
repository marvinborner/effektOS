import drivers
import capabilities/graphics

interface Syscall {
  def gpuContext(): GPUContext
}

def syscalls { prog: => Nothing / { Output, Syscall } }: Nothing / { Output, Global } =
  try prog()
  with Syscall {
    def gpuContext() = resume(graphics::context())
  }
