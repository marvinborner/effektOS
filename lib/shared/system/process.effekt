import set

type ProcessId = Int
record Process(id: ProcessId)

val SYSTEM = Process(0)

interface IPC[Task, Message, Filter] {
  def current(): Process
  def spawn(task: Task): Process
  def send(process: Process, message: Message): Unit
  def receive(on: Set[Filter]): Message // yield
}
